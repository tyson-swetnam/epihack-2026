{
  "_comment": "Paste the 'adhs' block into your Claude Desktop config under mcpServers. Restart Claude Desktop after editing. With no ADHS_BACKEND_URL set the server returns canned values sourced from ADHS PDFs and the EpiHack knowledge graph -- safe to demo without scraping ADHS dashboards.",
  "mcpServers": {
    "adhs": {
      "command": "uvx",
      "args": [
        "--from",
        "/absolute/path/to/epihack-2026/mcp/adhs-mcp",
        "adhs-mcp"
      ],
      "env": {
        "ADHS_BACKEND_URL": "",
        "ADHS_API_TOKEN": ""
      }
    }
  }
}
