source 'https://rubygems.org'

# GitHub Pages compatible Jekyll stack. The published version of the
# `github-pages` gem pins Jekyll + the plugin set so the workflow
# build matches what Pages would render server-side.
gem 'github-pages', group: :jekyll_plugins

group :jekyll_plugins do
  gem 'jekyll-relative-links'
end
